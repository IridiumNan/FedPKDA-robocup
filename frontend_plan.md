# 前端设计方案

> 内部设计文档。部分内容可直接用于后续架构白皮书和技术接口文档。

---

## 一、文档定位

本文档解决一个问题：**第一阶段前端要做什么、给谁做、为什么这样做。**

不涉及具体 UI 细节（线框图/配色/组件），那是下一阶段的事。

---

## 二、系统给谁用

### 角色 1：联盟协调员

- **典型身份**：行业监管机构技术负责人、CERT 联盟联络人
- **使用频率**：周期性巡检（非持续值守）
- **核心诉求**：
  - 联盟是否在正常运转？有没有节点掉线或异常？
  - 全局模型的检测率是否在下降？
  - 哪个节点可能需要关注或通知其负责人？
- **不能做的事**：不能查看任何成员机构的原始数据、不能查看成员上传的原型向量、不能强制操作成员节点

### 角色 2：机构运营员

- **典型身份**：某银行/电力公司 SOC 值班工程师
- **使用频率**：按需查看，出事时深入
- **核心诉求**：
  - 确认本机构数据没有离开自己的服务器
  - 联盟模型对自己机构的检测效果如何
  - 协调员有没有给自己的节点发建议或告警
  - 控制自己节点的参与策略（训练/评估/空闲）
- **不能做的事**：不能查看其他任何机构的信息

### 角色 3：合规审计员（本轮暂不设计页面）

- **典型身份**：第三方审计、监管合规人员
- **核心诉求**：验证隐私协议是否被执行、差分隐私参数是否在承诺范围内、协作过程是否有记录

---

## 三、核心创新：技术信任重塑权力关系

### 传统 ISAC 模式的问题

ISAC（Information Sharing and Analysis Center）是美国运行最成熟的行业威胁情报共享机制，但它有一个结构性缺陷：

- **信任是基于"你是谁"**：成员机构通过签署法律协议互相信任，协调员作为信息集散中心拥有最高数据访问权限
- **协调员本身是最大安全风险点**：他能看到所有共享数据，一旦被攻陷或被收买，整个联盟的数据暴露
- **共享依赖人工判断**：分析员逐条决定"能不能发"，慢且不一致
- **搭便车无法度量**：没有技术手段检测"只下载模型、不贡献数据"的行为

### FedPKDA 的技术信任

FedPKDA 从数学层面改变了这个结构：

| 维度 | ISAC 模式 | FedPKDA 模式 |
| ------ | ---------- | ------------- |
| 传输内容 | 原始或脱敏事件数据 | 裁剪+噪声原型（PSNR 6.9，数学不可逆） |
| 协调员能看什么 | 所有共享数据 | 仅聚合统计指标，看不到任何原始数据或原型 |
| 信任基础 | 法律协议 + 身份认证 | 算法保证"你技术上做不了坏事" |
| 异常检测 | 依赖人工举报或主动披露 | 马氏距离自动计算，无需任何一方举报 |
| 贡献度量 | 无法精确量化 | Fisher 信息矩阵天然量化，搭便车自动降权 |
| 共享决策 | 分析员人工判断 | 算法自动加噪上传，无需人工干预 |

### 结果：协调员角色退化（这是好事）

协调员从"必须绝对可信的信息集散中心"变成了一个**轻量的系统管理员**——能看到告警和统计，但技术上拿不到任何可以滥用的数据。这不是权限控制的改良，而是**架构本身就剥夺了滥用数据的可能性**。

---

## 四、页面结构

### 设计原则（来自 frontend_analysis.md）

- **直观与易用**：视觉自解释，禁止堆砌专业术语
- **实用性优先**：每一条信息都要能指导行动，每个告警都要有操作入口
- **视觉降噪**：核心信息放在醒目位置，辅助信息渐进式展开
- **明确边界**：聚焦网络安全垂直场景，不泛化

### 页面清单

| 页面 | 解决的用户问题 | 核心用户 |
| ------ | ------------- | --------- |
| **事件中心** | "系统发生了什么？要不要广播通知？" | 协调员（主工作台） |
| **节点管理** | "联盟有哪些机构？需要增删或通知特定机构吗？" | 协调员 |
| **节点状态** | "我的节点运行得怎么样？要不要调整参与策略？" | 运营员 |
| **训练控制** | "模型训练到什么程度了？需要调参吗？" | 协调员 + 运营员 |
| **安全事件** | "我机构被攻击了吗？怎么追溯？" | 运营员（协调员无权限） |
| **系统架构** | "这个系统怎么运作的？" | 所有角色 |

### 各页面信息架构

#### 事件中心（协调员视图）

协调员登录后的主工作台。核心是一个**可操作的事件日志时间线**，每一条都是系统自动检测到的、需要关注的具体事件。

顶部固定区域（持续监控指标）：

- **全局检测率**：当前值 + 趋势（↑/↓/→）
- **当前训练轮次**
- **活跃节点数**

主体区域：事件时间线（按时间倒序）

```
14:32  ⚠️ 节点 BANK-03 行为偏离 4.2σ（阈值 3.0σ）
      建议：通知该机构自查数据质量或检查是否被攻陷
      [广播通知各机构]  [标记已处理]

13:15  📉 全局检测率连续 5 轮下降（96.3% → 89.1%）
      建议：各节点增加本地训练轮次或检查数据漂移
      [广播通知各机构]  [调整全局参数]

09:47  🆕 系统检测到新型攻击模式（来源匿名）
      已自动纳入全局模型，建议各机构同步
      [广播通知各机构]  [标记已处理]

08:02  🔴 节点 ENG-02 已离线超过 3 轮
      最近响应：07:45
      建议：联系该机构负责人确认状态
      [广播通知各机构]  [标记已处理]
```

| 事件类型 | 算法来源 | 触发条件 |
| --------- | --------- | --------- |
| 节点行为偏离 | `compute_global_prototypes()` 马氏距离 | 某节点距离 > 3σ |
| 检测率下降 | `rs_test_acc` 趋势分析 | 连续 N 轮下降超过 X% |
| 新攻击模式检出 | 客户端本地推理 | 未知类别检测置信度 > 阈值 |
| 节点离线 | 客户端心跳超时 | N 轮未收到该节点响应 |
| 训练轮次完成 | 服务端训练循环 | 每轮结束自动记录 |

每条事件的脱敏规则：不暴露具体数据值（如原始原型向量），只描述偏离程度和影响范围。

操作按钮说明：

- **广播通知**：将该事件（脱敏后）推送给所有成员机构运营员，让各家知晓并自行决定是否调整策略
- **标记已处理**：该事件不再高亮，移入已处理列表
- **调整全局参数**：仅对可参数化应对的事件（如检测率下降时调整训练参数）

节点状态概览**不放在此页面**——协调员需要管理节点时，导航到"节点管理"页面操作。

#### 节点管理（协调员视图）

协调员管理联盟成员机构的入口。不涉及单个节点的内部运行细节，只关注机构层面的协作关系。

每行一个机构，展示：

- 机构名称 + 行业标识（银行/电力/政务）
- 节点状态圆点（在线/离线/被建议隔离）
- 最近活跃时间
- 行为评分（基于马氏距离，仅显示综合分数，不展示内部细节）
- 该机构负责人联系方式（电话号码，点开展示）

操作：

- **[添加机构]**：邀请新机构加入联盟
- **[移除机构]**：将机构移出联盟
- **[建议隔离]** / **[取消建议]**：标记建议后，该信息会推送到事件中心并通知该机构运营员

协调员在此页面**不能**看到任何节点的训练数据、本地检测结果、本地原型。

#### 节点状态（运营员视图）

运营员查看和管理**自己机构节点**的完整运行状态。这是运营员看到信息最详细的页面。

展示内容：

- 节点运行状态：在线/离线、当前角色（训练中/评估中/空闲）
- 训练指标：本地训练轮次、本地损失值、本地检测率
- 隐私保护状态：当前噪声注入强度、ε 值、数据传输加密状态
- 模型同步状态：最近一次接收全局模型的时间、本地-全局模型版本号
- 行为评分（自己节点的完整评分，包含偏离度的详细说明）
- 协调员发来的建议/告警（如有）

操作：

- **[隔离]** / **[恢复]**：控制自己节点是否参与本轮全局聚合
- **[配置参与策略]**：切换训练/评估/空闲模式
- **[调整本地参数]**：修改本地噪声强度、local_par、训练轮次等
- **[查看事件详情]**：跳转到安全事件页面查看本机构的相关事件

运营员**只能看到自己机构的信息**，看不到任何其他机构的数据。

#### 训练控制

两个角色进入同一个页面，但看到的信息和可调参数不同。训练是持续运行的，不需要手动开始/停止——协调员在事件中心发现异常后，来此页面调参。

**共同可见区域**：

- 全局检测率趋势折线图（横轴轮次，纵轴检测率，用已跑完的 `experiment_data.json`）
- 当前训练轮次 / 总轮次、每轮耗时

**协调员专属**：

- 全局参数面板：
  - `global_par` / `local_par`（影响所有节点的本地-全局对齐平衡）
  - 每轮参与节点比例（join_ratio）
  - 异常检测阈值（σ，多敏感算"可疑"）
- 所有节点的贡献度排行（Fisher 权重，含历史趋势）

**运营员专属**：

- 本地检测率 vs 全局检测率对比曲线
- 自己节点的贡献度评分 + 历史趋势
- 本地参数面板：
  - 噪声注入强度（Laplace scale）
  - 本地训练轮次
  - 参与模式切换（训练 / 评估 / 空闲）

**暂不实现（第二阶段）**：

- 模型快照与回滚——运营员发现本地训练跑偏后回退到历史干净版本。需要后端持久化存储和版本管理，超出本轮资源

#### 安全事件（仅运营员）

**协调员没有此页面权限。** 原因：

- 被攻击是机构的声誉风险——成员机构不希望任何外部方知道自己被攻击过
- 算法本身已经解决了知识传播问题：攻击检测 → 原型更新 → 全局聚合 → 所有成员自动受益，不需要任何人知道攻击发生在哪家机构
- 协调员在事件中心只能看到一个完全匿名的通知："系统检测到新型攻击模式，已纳入全局模型"

运营员看到的内容：

- **攻击检测时间线**：攻击类型 + 置信度 + 检测时间 + 影响范围（仅自己机构内）
- **每条的算法来源**：全局模型在本地数据上的推理结果——哪一轮的模型检出的、置信度多少
- **攻击详情展开**：点击某条可查看完整流量特征摘要（不暴露原始载荷）
- **操作**：[标记已处理] / [导出报告]

与事件中心的联动：

- 当全局模型首次检测到新型攻击模式时，事件中心生成一条匿名通知："系统检测到新型攻击模式，已纳入全局模型。建议各机构同步攻击库。"
- 该通知不包含：哪个机构检出的、攻击类型名称、检出时间
- 机构运营员在自己的安全事件页面可以看到自己机构检测到的该攻击的完整信息

#### 系统架构

面向所有角色。回答两个问题：**系统怎么运作的？为什么这样设计？**

##### 核心工作流程

每轮训练是一个固定的循环：

```
① 本地训练
   各机构在自己的网络流量数据上训练本地特征提取器

② 原型提取
   从特征提取器输出中计算每个攻击类别的特征中心

③ 加噪上传
   裁剪到 [-1,1] + 拉普拉斯噪声 → 上传到中心服务器
   （原始数据从未离开机构服务器）

④ 服务端聚合
   K-Means 聚类 → 马氏距离计算 → 距离加权平均
   → 生成全局原型         ↗ 异常节点自动降权

⑤ 全局原型广播
   全局原型发回各机构

⑥ 双对齐训练
   各机构用全局原型做本地对齐 (L_LA) + 全局对齐 (L_GA)
   φ(t) 自动调节两者权重（早期偏本地个性化，后期偏全局泛化）

⑦ Fisher 加权聚合模型
   各节点的模型贡献度天然量化，搭便车自动降权

⑧ 进入下一轮
```

##### 事件触发点

| 在哪一步 | 触发事件 | 进入事件中心 |
| --------- | --------- | ------------ |
| 步骤④ | 某节点马氏距离 > 3σ | "节点 X 行为偏离" |
| 步骤④ | 某节点 N 轮未响应 | "节点 X 已离线" |
| 步骤④ | 每轮聚合完成 | "第 N 轮训练完成" |
| 步骤⑧ | 连续 N 轮检测率下降 | "检测率持续下降" |
| 步骤② | 本地模型检出未知攻击 | "新攻击模式（匿名）" |

##### 角色可见边界

```
                  协调员看这里
                ┌──────┼────────┐
                │ 事件中心      │
                │ 节点管理      │← 只有机构列表和综合评分
                │ 训练控制      │← 只有全局参数和聚合趋势
                └───────────────┘
                       ↑
                 聚合指标 + 脱敏事件
                       ↑
            ┌────────────────────┐
            │   中心服务器       │
            │  K-Means + 马氏距离│← 服务器拿到加噪原型用于聚合
            │  Fisher 加权聚合   │    但协调员看不到这些
            └───────┬──┬──┬──────┘
                    ↑  ↑  ↑  加噪原型
               ┌────┘  │  └────────┐
               │       │           │
         ┌─────┴──┐ ┌──┴─────┐ ┌───┴────┐
         │机构 A  │ │机构 B  │ │机构 C  │
         │原始数据│ │原始数据│ │原始数据│← 每个机构只能看自己的
         │本地模型│ │本地模型│ │本地模型│
         │安全事件│ │安全事件│ │安全事件│
         └────────┘ └────────┘ └────────┘
```

##### 为什么这个系统有价值？

每个机构都需要入侵检测，但单家机构能看到的攻击样本太少。联盟的价值：**别人被打过的东西，你的模型能学会防御；但别人不会知道你被打过。**

| 利益 | 实现机制 | 为什么可以信任 |
| ------ | --------- | ------------- |
| 模型效果提升 | 全局原型聚合 + 双对齐 | 见过自己没见过的攻击模式 |
| 个性化保留 | φ(t) 动态调节，早期偏本地 | 银行模型不会因电力数据退化 |
| 搭便车不被容忍 | Fisher 加权聚合 | 贡献低的节点自动降权 |
| 知识自动传播 | 攻击检测 → 原型更新 → 全局聚合 | 不需要任何人主动报告 |

##### 风险怎么消除？

利益实现和风险消除是同一套机制——不是两件事。

| 风险 | 消除机制 | 是算法还是架构？ |
| ------ | --------- | ---------------- |
| 原始数据泄露 | 传输原型而非梯度（PSNR 6.9，数学不可逆） | 算法 |
| 恶意节点污染全局模型 | 马氏距离自动隔离异常原型 | 算法 |
| 机构被攻陷不自知 | 马氏距离自动标记，事件中心告警 | 算法 |
| 协调员成为数据集中风险点 | 协调员技术上拿不到原型和数据 | 架构 |
| 被攻击的声誉风险 | 安全事件仅自己可见，协调员只能看匿名通知 | 架构 |
| 隐私合规风险 | 内置拉普拉斯噪声，ε 可配置可审计 | 算法 |
| 节点掉线无人知道 | 心跳超时 → 事件中心告警 | 架构 |

##### 与传统 ISAC 的本质区别

ISAC 靠法律协议区分"能做什么"和"不能做什么"——信任基于"你是谁"。
FedPKDA 靠数学和架构让"不能做"变成"做不了"——信任基于"你技术上做不了坏事"。

协调员从"必须绝对可信的信息集散中心"退化成一个轻量的系统管理员——即使协调员被攻陷，也拿不到任何可滥用的数据。

---

## 五、前端-算法映射

### 算法输出 → 前端展示

| FedPKDA 内部计算 | 产出 | 前端展示 |
| --- | --- | --- |
| `compute_global_prototypes()` 马氏距离 | 每个客户端的异常分数 | 节点行为评分（0-100） |
| `rs_test_acc` | 每轮全局测试精度 | 全局检测率 + 趋势折线图 |
| `Func` φ(t) | 当前个性化/全局化程度 | 训练阶段指示器 |
| Fisher 信息聚合 | 每个客户端的贡献权重 | 节点贡献度 |
| `get_noisy_local_prototypes()` 噪声参数 | Laplace 噪声尺度 | 隐私保护强度指示器 |
| 全局模型本地推理 | 攻击检测结果 | 安全事件时间线 |

### 用户操作 → 算法参数

| 用户操作 | 对应的算法变更 |
| --------- | ------------- |
| 协调员调整 global_par | 修改 `set_parameters()` 中的全局对齐权重 |
| 运营员隔离自己的节点 | 该节点不上传原型到 `compute_global_prototypes()` |
| 运营员调整噪声强度 | 修改 `get_noisy_local_prototypes()` 的 Laplace scale |
| 运营员切换训练/评估/空闲 | 改变该客户端在 `send_selected_models()` 中的身份 |
| 协调员建议隔离 | 不改变算法，仅生成通知记录 |

---

## 六、技术选型

### 推荐：Streamlit

- 纯 Python，不需要前端工程师
- 数据绑定简单（直接 `json.load` 实验数据）
- 快速出交互原型，适合 Demo 阶段
- 缺点：复杂拓扑图/动画不如 JS 框架灵活

### 替代方案（如果需要更灵活的视觉效果）

- Dash + Plotly（Python，图表能力强，布局控制更精细）
- 纯 HTML + vanilla JS（如果那个会前端的队友能按我们的信息架构重做）

### Demo 阶段数据策略

- 全局态势的检测率曲线：使用已跑完的四组 Cifar100 实验数据（`experiment_data.json`）
- 节点状态、行为评分、安全事件：使用模拟数据，但 schema 与真实算法输出一致，保证后续接后端时前端不用改

---

## 七、与 legacy 前端的关系

`legacy_frontend/` 下的 6 个 HTML 文件**不适用**，原因：

- 信息全是 FL 通用术语，没有翻译成运营语言
- 没有回答"运营人员需要做什么决定"
- 硬编码数据，无真实数据绑定
- 视觉元素堆砌，重点不突出

**可复用部分**：暗色科技风 CSS 样式可作为视觉皮肤参考，但信息架构和交互逻辑需完全重做。

---

## 八、阶段划分

### 第一阶段（本轮实现）

- [ ] 事件中心：事件时间线 + 顶部监控指标 + 广播通知/标记已处理
- [ ] 节点管理（协调员）：机构列表 + 增删/建议隔离 + 联系方式
- [ ] 节点状态（运营员）：自己节点的完整运行状态 + 参与策略控制
- [ ] 训练控制：检测率趋势图 + 全局/本地参数面板（协调员与运营员参数分离）
- [ ] 安全事件：运营员的攻击检测时间线
- [ ] 系统架构：交互式数据流图 + 五大突破对比
- [ ] 模拟数据 schema 定义（与真实算法输出结构一致）

### 第二阶段（后续迭代）

- [ ] 模型快照与回滚功能（训练控制页面）
- [ ] 合规审计员页面
- [ ] 真实后端对接（替换模拟数据为 Flower 框架输出）
- [ ] 权限系统（角色切换）

### 待定

- [ ] 各页面的具体线框图
- [ ] 每页对应的后端接口定义（从 UI 元素反推）
- [ ] 安全事件页面的详细交互设计
- [ ] Streamlit 多页应用的项目结构
